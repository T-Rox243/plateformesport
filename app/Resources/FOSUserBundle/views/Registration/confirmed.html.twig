{% extends "FOSUserBundle::layout.html.twig" %}

{% block fos_user_content %}
	{% block body %}
	    <div id="wrapper">
	        <div class="container">
	            <div class="row">
		            <div class="col-md-6 col-md-offset-3 confirmSignUp">
					    <p>Votre inscription a été prise en compte</p>
					    <p>Vous pouvez maintenant créer vos sports ou vos événements !</p>
				    </div>
				</div>
	            <div class="row">
		            <div class="col-md-3 col-md-offset-3">
					    <a href="{{ path('homepage') }}"><input class="btn btn-success btn-block" type="button" value="Retourner à l'accueil"></a>
				    </div>
		            <div class="col-md-3">
					    <a href="{{ path('infoUser', {'idUser': 12}) }}"><input class="btn btn-success btn-block" type="button" value="Voir mon compte"></a>
				    </div>
				</div>
	        </div>
	    </div>
	{% endblock %}
{% endblock fos_user_content %}
